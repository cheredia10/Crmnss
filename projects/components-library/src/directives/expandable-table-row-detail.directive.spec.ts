import { ExpandableTableRowDetailDirective } from './expandable-table-row-detail.directive';

describe('ExpandableTableRowDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableTableRowDetailDirective();
    expect(directive).toBeTruthy();
  });
});
